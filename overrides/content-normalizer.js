(function(){
'use strict';
if(window.__scContentNormalizerBooted)return;
window.__scContentNormalizerBooted=true;
/* The static snapshot is the content source of truth. Do not rewrite casing,
   punctuation, emphasis or product names at runtime from an override layer. */
})();
